const resolvers = {
    RootQuery: {
      categories: () => {
        // Resolver function for retrieving categories and items
        // This is a placeholder. In a real application, fetch data from the database.
      },
    },
  };
  
  export default resolvers;